# 🚀 DevStack

A modern technology stack builder built with React and TypeScript.

## 🛠️ Technologies Used

- HTML
- CSS
- TypeScript
- React
- TailWind CSS

## ✨ Features

- You can add technologies in 'Your Stack'
- You can remove single technologies from 'Your Stack'
- You can remove all technologies from 'Your Stack'

## Question Answers

**I. What is JSX, and why is it used in React?**

- **Answer:** JSX is a syntax extension that looks like HTML. It is used in React to make writing and visualizing component structures much easier.

**II. What is the difference between props and state?**

- **Answer:** **Props** are used to pass data down from a parent component, while **state** is used to manage and update data locally within a component.

**III. What does the useState hook do, and where did you use it in this project?**

- **Answer:** **useState** update the data and it should use in the parent

**IIII. What does the useEffect hook do, and why did you need it to load the JSON data?**

- **Answer:** In React, the **useEffect** hook is a built-in tool that allows functional components to perform side effects.If you attempt to load or fetch JSON data directly inside the main body of a React component function, you will run into severe logic and performance bugs. You need useEffect for two major reasons

**v. Why does every item in a .map() list need a unique key prop?**

- **Answer:** **.map()** need a unique because to identify each item uniquely.

**vi. What is conditional rendering? Show one place you used it (example: the empty stack message).**

- **Answer:** **conditional rendering** is situation when something happen based on the condition.

**vii. How do you pass data from a parent component to a child component, and how does a child send something back to the parent?**

- **Answer:** I pass data from a parent component to a child by props and I back something from child by passing props value.
